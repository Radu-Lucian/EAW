{% extends 'base.html.twig' %}

{% block title %}InventoryItems{% endblock %}

{% block body %}
    <h1>InventoryItems</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inventory_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ inventory_item.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ inventory_item.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ inventory_item.price }}</td>
            </tr>
            <tr>
                <th>Serial_number</th>
                <td>{{ inventory_item.serialNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inventory_items_index') }}">back to list</a>

    <a href="{{ path('inventory_items_edit', {'id': inventory_item.id}) }}">edit</a>

    {{ include('inventory_items/_delete_form.html.twig') }}
{% endblock %}
