{% extends 'base.html.twig' %}

{% block title %}UserTicketsMechanics{% endblock %}

{% block body %}
    <h1>UserTicketsMechanics</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_tickets_mechanic.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_tickets_mechanics_index') }}">back to list</a>

    <a href="{{ path('user_tickets_mechanics_edit', {'id': user_tickets_mechanic.id}) }}">edit</a>

    {{ include('user_tickets_mechanics/_delete_form.html.twig') }}
{% endblock %}
