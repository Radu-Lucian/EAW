{% extends 'base.html.twig' %}

{% block title %}UserTicketsMechanics index{% endblock %}

{% block body %}
    <h1>UserTicketsMechanics index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_tickets_mechanic in user_tickets_mechanics %}
            <tr>
                <td>{{ user_tickets_mechanic.id }}</td>
                <td>
                    <a href="{{ path('user_tickets_mechanics_show', {'id': user_tickets_mechanic.id}) }}">show</a>
                    <a href="{{ path('user_tickets_mechanics_edit', {'id': user_tickets_mechanic.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('user_tickets_mechanics_new') }}">Create new</a>
{% endblock %}
