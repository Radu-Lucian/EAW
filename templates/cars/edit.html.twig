{% extends 'base.html.twig' %}

{% block title %}{{"Edit the selected car"|trans}}{% endblock %}

{% block body %}
    <br>
    <center><h1>{{"Edit the selected car"|trans}}</h1></center>

    <div class="container">
        {{ include('cars/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <center>
		<button class="btn btn-outline-secondary" type="button" onclick="location.href='{{ path('cars_index') }}'">{{"Back to list"|trans}}</button>
	</center>

    <br>
    {{ include('cars/_delete_form.html.twig') }}
{% endblock %}
